# =============================================================================
# storefront/.env.production.template
#
# Copy to storefront/.env.production and fill in all values before running
# `next build` outside of Docker (e.g. bare-metal or PaaS).
#
# When using docker-compose.prod.yml these values are passed as Docker build
# args and you do NOT need a separate .env.production file.
#
# NOTE: NEXT_PUBLIC_* variables are embedded into the JavaScript bundle at
# build time. Changing them requires a full rebuild of the Next.js app.
#
# Generate secrets with: openssl rand -hex 32
# =============================================================================

# Public URL of the Medusa backend reachable from the browser
NEXT_PUBLIC_MEDUSA_BACKEND_URL=https://your-backend.example.com

# Publishable API key – obtain from Medusa Admin:
#   https://your-backend.example.com/app/settings/publishable-api-keys
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=

# Public URL of this storefront
NEXT_PUBLIC_BASE_URL=https://your-storefront.example.com

NEXT_PUBLIC_DEFAULT_REGION=us

# Next.js on-demand revalidation secret – generate with: openssl rand -hex 32
REVALIDATE_SECRET=
