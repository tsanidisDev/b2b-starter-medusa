# --- storefront/.dockerignore ---
# Keeps the build context lean: only source + manifests reach the daemon.

# Dependencies (reinstalled inside the image)
node_modules

# Next.js build output (rebuilt inside the image by `yarn build`)
.next
out

# Environment files — secrets / NEXT_PUBLIC_ vars are injected via build ARGs, not files
.env
.env.*
!.env.example

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test tooling
coverage
*.test.ts
*.spec.ts

# Editor / OS noise
.DS_Store
.idea
.vscode
*.swp
*.swo
Thumbs.db

# Git
.git
.gitignore
.gitattributes

# Docs
README.md
