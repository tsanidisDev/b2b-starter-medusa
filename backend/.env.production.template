# =============================================================================
# backend/.env.production.template
#
# Copy to backend/.env.production and fill in all values before running
# Medusa in production outside of Docker (e.g. bare-metal or PaaS).
#
# When using docker-compose.prod.yml these variables are injected via the
# compose file and you do NOT need a separate .env.production file.
#
# Generate secrets with:  openssl rand -hex 32
# =============================================================================

# --- Database -----------------------------------------------------------------
DATABASE_URL=postgres://postgres:CHANGE_ME@localhost:5432/medusa-store

# --- Redis --------------------------------------------------------------------
REDIS_URL=redis://localhost:6379

# --- Authentication secrets ---------------------------------------------------
# REQUIRED – must NOT be the default "supersecret" in production
JWT_SECRET=
COOKIE_SECRET=

# --- CORS (comma-separated list of allowed origins) ---------------------------
STORE_CORS=https://your-storefront.example.com
ADMIN_CORS=https://your-admin.example.com
AUTH_CORS=https://your-admin.example.com
