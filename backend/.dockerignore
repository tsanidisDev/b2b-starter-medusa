# --- backend/.dockerignore ---
# Keeps the build context lean: only source + manifests reach the daemon.

# Dependencies (reinstalled inside the image)
node_modules

# Compiled output (rebuilt inside the image by `yarn build`)
.medusa
dist

# Test / dev tooling
coverage
integration-tests
jest.config.js
*.test.ts
*.spec.ts

# Environment files — secrets must never enter an image layer
.env
.env.*
!.env.example

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS noise
.DS_Store
.idea
.vscode
*.swp
*.swo
Thumbs.db

# Git
.git
.gitignore
.gitattributes

# Docs (not needed at runtime)
README.md
DEPLOYMENT.md
docs/
